namespace CodeBase
{
    public enum PanelsUI
    {
        Menu = 0,
        Game = 1,
        Lose = 2
    }

    public enum Events
    {
        OnTakeStone = 0,
        OnTap = 1,
        OnLose = 2
    }
}

